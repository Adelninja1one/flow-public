# FLOW Public Site

This repository contains only the public static introduction page for FLOW. It must never contain source code, environment variables, shipment data, customer information, maps, API URLs with secrets, mobile signing keys, or backend configuration.
